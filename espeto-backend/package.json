{
  "name": "espeto-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npx prisma migrate deploy && npx prisma generate"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}